import React from 'react';
import './App.css';

function App() {
  return (
    <div className="App">
      <header className="App-header">
        React + Firebase Hosting + GCP Cloud Build then Applying the GCP Cloud Build CI/CD Pipeline
      </header>
    </div>
  );
}

export default App;
